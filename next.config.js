/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: false
}

module.exports = nextConfig

module.exports = {
  images: {
    domains: ['img.icons8.com'],  // Add the external image domain here
  },
};